# Railway Configuration for Client
# This file tells Railway to use Railpack (the new native builder) instead of the Dockerfile.

[build]
builder = "RAILPACK"
buildCommand = "npm run build"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100

